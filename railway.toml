[build]
builder = "dockerfile"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
# Railway 自动从 GitHub main 分支部署，每次 push 触发重新构建
# v3.0 - 端口 8080 匹配自定义域名 wangys666.top (2026-08-13)
